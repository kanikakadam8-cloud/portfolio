# Phase 2+ secrets. Copy to job-agent/.env (gitignored) and fill in.
# Also add these as GitHub repo secrets (Settings → Secrets → Actions) for the cron.

# Adzuna (free: https://developer.adzuna.com) — Phase 2, India coverage
ADZUNA_APP_ID=
ADZUNA_APP_KEY=
