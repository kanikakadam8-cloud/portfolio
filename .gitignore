# Unused / unlinked images — kept locally, not pushed
work-experience/images/other-images/

# AI-generated experiments (source folders) — kept locally, not pushed.
# The deployed AI versions live in work-experience/images-ai/ and ARE tracked.
**/ai images/
**/Ai images/
**/Ai folder/

# Internal H2L work docs — local only, NOT for the public portfolio repo.
# (Remove this line if you intend to deploy/publish the work/ folder.)
work/

# OS / editor cruft
desktop.ini
.DS_Store
Thumbs.db
*.tmp

# Local AI-tooling config
.claude/

# Private financial documents — standalone, never publish to the portfolio repo
bills/

# Job-agent secrets (Phase 2+). Everything else in /job-agent IS tracked.
job-agent/.env
job-agent/node_modules/
# Generated dashboard data — force-added to the job-agent-data branch by CI only
job-agent/dashboard/
